-- ============================================================
-- Migration 029: итоговый документ «Что делаем» на пачку
--
-- document — AI-сборка (Claude) из ту-ду + разбора недели + шторма + параметров
-- в ОДИН структурный документ направлений на следующую пачку крео. Дальше из него
-- собираются ТЗ на крео. Хранится в снапшоте недели.
-- ============================================================

alter table weekly_creative_reports add column if not exists document text not null default '';
